# V1.11 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Polish-Release nach V1.10 — die größten Views entlasten, die Widget-Snapshot-Pipeline aus dem View-Lifecycle ziehen und das Katalog-Bibliothek-Matching auf eine Quelle der Wahrheit konsolidieren.

**Architecture:** Reine Logik wird aus SwiftUI-Views in testbare, UI-freie Typen extrahiert (`EpisodeEditDraft`, `EpisodeEditSaveHandler`, `EpisodeEditCoverHandler`, `WidgetSnapshotCoordinator`, `CatalogLibraryMatcher`). Views bleiben als dünne Orchestratoren zurück. Kein SchemaV7, keine Verhaltensänderung außer einem Whitespace-Normalisierungs-Fix in Phase 3.

**Tech Stack:** Swift 6, SwiftUI, SwiftData, XCTest. Min iOS 17. Test-Destination: `iPhone 17, OS 26.5`.

**Design doc:** `docs/2026-05-29-v1.11-design.md`

**Build/Test commands:**
```bash
# Einzelne Testklasse
xcodebuild -project EpisodeTracker.xcodeproj -scheme EpisodeTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' \
  test -only-testing:EpisodeTrackerTests/<ClassName> 2>&1 | tail -30

# Voller Testlauf
xcodebuild -project EpisodeTracker.xcodeproj -scheme EpisodeTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' test 2>&1 | tail -30
```

**Test in-memory container pattern** (aus `BootstrapReportTests.swift`):
```swift
private func makeInMemoryContext() throws -> ModelContext {
    let schema = Schema([Episode.self, Mood.self, Universe.self])
    let config = ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)
    let container = try ModelContainer(for: schema, configurations: [config])
    return ModelContext(container)
}
```

> **Xcode-Projektdatei:** Neue `.swift`-Dateien müssen ins richtige Target. Das Projekt nutzt synchronized groups (Dateien im Ordner werden automatisch erfasst). Neue Dateien an den im Task genannten Pfad legen; nach dem Erstellen `xcodebuild` bauen, um die Target-Zugehörigkeit zu verifizieren. Test-Dateien gehören ins Target `EpisodeTrackerTests`.

---

## Phase 1 — EpisodeEditView entlasten

`EpisodeEditView.swift` (940 Zeilen) wird auf ~400 Zeilen reduziert, indem Draft, Save-Logik und Cover-Handling in drei Typen wandern.

### Task 1: EpisodeEditDraft — Formularzustand und Parsing

**Files:**
- Create: `EpisodeTracker/Features/EpisodeEdit/EpisodeEditDraft.swift`
- Test: `EpisodeTrackerTests/EpisodeEditDraftTests.swift`

- [ ] **Step 1: Write the failing test**

```swift
// EpisodeTrackerTests/EpisodeEditDraftTests.swift
import XCTest
@testable import EpisodeTracker

final class EpisodeEditDraftTests: XCTestCase {
    func testParsesNumberAndYearIgnoringWhitespace() {
        var draft = EpisodeEditDraft()
        draft.episodeNumberText = " 42 "
        draft.releaseYearText = "1979"

        XCTAssertEqual(draft.parsedEpisodeNumber, 42)
        XCTAssertEqual(draft.parsedReleaseYear, 1979)
    }

    func testParsedValuesAreNilForNonNumericInput() {
        var draft = EpisodeEditDraft()
        draft.episodeNumberText = "abc"
        draft.releaseYearText = ""

        XCTAssertNil(draft.parsedEpisodeNumber)
        XCTAssertNil(draft.parsedReleaseYear)
    }

    func testIsCompleteRequiresTitleNumberYearAndUniverse() {
        let universe = Universe(name: "Die drei ???")
        var draft = EpisodeEditDraft()
        XCTAssertFalse(draft.isComplete)

        draft.title = "Der Super-Papagei"
        draft.episodeNumberText = "1"
        draft.releaseYearText = "1979"
        XCTAssertFalse(draft.isComplete, "Universe fehlt noch")

        draft.selectedUniverse = universe
        XCTAssertTrue(draft.isComplete)
    }

    func testInitFromEpisodeCopiesAllFields() {
        let universe = Universe(name: "TKKG")
        let mood = Mood(name: "spannend")
        let episode = Episode(
            episodeNumber: 7,
            title: "Die Bettelmönche",
            releaseYear: 1981,
            personalNote: "Klassiker",
            isListened: true,
            rating: 4,
            universe: universe,
            moods: [mood]
        )
        episode.streamingURL = "https://example.com"
        episode.isHidden = true

        let draft = EpisodeEditDraft(episode: episode, universes: [universe])

        XCTAssertEqual(draft.title, "Die Bettelmönche")
        XCTAssertEqual(draft.episodeNumberText, "7")
        XCTAssertEqual(draft.releaseYearText, "1981")
        XCTAssertEqual(draft.personalNote, "Klassiker")
        XCTAssertTrue(draft.isListened)
        XCTAssertEqual(draft.rating, 4)
        XCTAssertEqual(draft.streamingURL, "https://example.com")
        XCTAssertTrue(draft.isHidden)
        XCTAssertEqual(draft.selectedMoods, [mood])
        XCTAssertEqual(draft.selectedUniverse, universe)
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/EpisodeEditDraftTests`
Expected: FAIL — `cannot find 'EpisodeEditDraft' in scope`

- [ ] **Step 3: Write minimal implementation**

```swift
// EpisodeTracker/Features/EpisodeEdit/EpisodeEditDraft.swift
import Foundation

struct EpisodeEditDraft {
    var episodeNumberText: String = ""
    var title: String = ""
    var releaseYearText: String = ""
    var personalNote: String = ""
    var isListened: Bool = false
    var rating: Int?
    var selectedMoods: Set<Mood> = []
    var selectedUniverse: Universe?
    var streamingURL: String = ""
    var isHidden: Bool = false

    init() {}

    /// Bestehende Folge in den Formularzustand laden.
    init(episode: Episode, universes: [Universe]) {
        episodeNumberText = String(episode.episodeNumber)
        title = episode.title
        releaseYearText = String(episode.releaseYear)
        personalNote = episode.personalNote ?? ""
        isListened = episode.isListened
        rating = episode.rating
        streamingURL = episode.streamingURL ?? ""
        isHidden = episode.isHidden
        selectedMoods = Set(episode.moods)
        selectedUniverse = episode.universe ?? universes.first
    }

    var parsedEpisodeNumber: Int? {
        Int(episodeNumberText.trimmingCharacters(in: .whitespacesAndNewlines))
    }

    var parsedReleaseYear: Int? {
        Int(releaseYearText.trimmingCharacters(in: .whitespacesAndNewlines))
    }

    var isComplete: Bool {
        !title.isEmpty
        && parsedEpisodeNumber != nil
        && parsedReleaseYear != nil
        && selectedUniverse != nil
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/EpisodeEditDraftTests`
Expected: PASS (4 Tests)

- [ ] **Step 5: Commit**

```bash
git add EpisodeTracker/Features/EpisodeEdit/EpisodeEditDraft.swift EpisodeTrackerTests/EpisodeEditDraftTests.swift
git commit -m "Add EpisodeEditDraft for episode form state"
```

---

### Task 2: EpisodeEditSaveHandler — Persistierung und Validierung

Extrahiert `save()` (Zeilen 396–481), `applyCoverChange` (483–494) und `hasDuplicateEpisodeNumber` (496–505) aus `EpisodeEditView`. Die Freemium-Schranke bleibt in der View (braucht `@Query`-Count + `@AppStorage`) und wird vor dem Aufruf geprüft.

**Files:**
- Create: `EpisodeTracker/Features/EpisodeEdit/EpisodeEditSaveHandler.swift`
- Test: `EpisodeTrackerTests/EpisodeEditSaveHandlerTests.swift`

- [ ] **Step 1: Write the failing test**

```swift
// EpisodeTrackerTests/EpisodeEditSaveHandlerTests.swift
import XCTest
import SwiftData
@testable import EpisodeTracker

final class EpisodeEditSaveHandlerTests: XCTestCase {
    private func makeInMemoryContext() throws -> ModelContext {
        let schema = Schema([Episode.self, Mood.self, Universe.self])
        let config = ModelConfiguration(schema: schema, isStoredInMemoryOnly: true)
        let container = try ModelContainer(for: schema, configurations: [config])
        return ModelContext(container)
    }

    func testSaveInsertsNewEpisode() throws {
        let context = try makeInMemoryContext()
        let universe = Universe(name: "Die drei ???")
        context.insert(universe)

        var draft = EpisodeEditDraft()
        draft.title = "Der Super-Papagei"
        draft.episodeNumberText = "1"
        draft.releaseYearText = "1979"
        draft.selectedUniverse = universe

        let outcome = EpisodeEditSaveHandler.save(
            draft: draft,
            existingEpisode: nil,
            existingEpisodes: [],
            coverChange: .keep,
            in: context
        )

        XCTAssertEqual(outcome, .saved)
        let stored = try context.fetch(FetchDescriptor<Episode>())
        XCTAssertEqual(stored.count, 1)
        XCTAssertEqual(stored.first?.title, "Der Super-Papagei")
        XCTAssertEqual(stored.first?.episodeNumber, 1)
    }

    func testSaveRejectsDuplicateNumberInSameUniverse() throws {
        let context = try makeInMemoryContext()
        let universe = Universe(name: "Die drei ???")
        context.insert(universe)
        let existing = Episode(episodeNumber: 1, title: "Bestand", releaseYear: 1979, universe: universe)
        context.insert(existing)

        var draft = EpisodeEditDraft()
        draft.title = "Neu"
        draft.episodeNumberText = "1"
        draft.releaseYearText = "1980"
        draft.selectedUniverse = universe

        let outcome = EpisodeEditSaveHandler.save(
            draft: draft,
            existingEpisode: nil,
            existingEpisodes: [existing],
            coverChange: .keep,
            in: context
        )

        XCTAssertEqual(outcome, .duplicateNumber)
        let stored = try context.fetch(FetchDescriptor<Episode>())
        XCTAssertEqual(stored.count, 1, "Keine neue Folge angelegt")
    }

    func testSaveUpdatesExistingEpisodeAndSetsListenTimestamps() throws {
        let context = try makeInMemoryContext()
        let universe = Universe(name: "TKKG")
        context.insert(universe)
        let episode = Episode(episodeNumber: 5, title: "Alt", releaseYear: 1981, universe: universe)
        context.insert(episode)

        var draft = EpisodeEditDraft(episode: episode, universes: [universe])
        draft.title = "Neu"
        draft.isListened = true

        let outcome = EpisodeEditSaveHandler.save(
            draft: draft,
            existingEpisode: episode,
            existingEpisodes: [episode],
            coverChange: .keep,
            in: context
        )

        XCTAssertEqual(outcome, .saved)
        XCTAssertEqual(episode.title, "Neu")
        XCTAssertTrue(episode.isListened)
        XCTAssertEqual(episode.listenCount, 1)
        XCTAssertNotNil(episode.lastListenedAt)
        XCTAssertNotNil(episode.listenStatusUpdatedAt)
    }

    func testSaveClearsBookmarkWhenMarkedListened() throws {
        let context = try makeInMemoryContext()
        let universe = Universe(name: "TKKG")
        context.insert(universe)
        let episode = Episode(episodeNumber: 5, title: "Alt", releaseYear: 1981, universe: universe)
        episode.isBookmarked = true
        context.insert(episode)

        var draft = EpisodeEditDraft(episode: episode, universes: [universe])
        draft.isListened = true

        _ = EpisodeEditSaveHandler.save(
            draft: draft,
            existingEpisode: episode,
            existingEpisodes: [episode],
            coverChange: .keep,
            in: context
        )

        XCTAssertFalse(episode.isBookmarked)
        XCTAssertNotNil(episode.bookmarkedUpdatedAt)
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/EpisodeEditSaveHandlerTests`
Expected: FAIL — `cannot find 'EpisodeEditSaveHandler' in scope`

- [ ] **Step 3: Write minimal implementation**

```swift
// EpisodeTracker/Features/EpisodeEdit/EpisodeEditSaveHandler.swift
import Foundation
import SwiftData

enum EpisodeSaveOutcome: Equatable {
    case saved
    case duplicateNumber
    case saveFailed
    case invalidInput
}

enum EpisodeEditSaveHandler {
    /// Persistiert den Draft als neue oder bestehende Folge.
    /// Die Freemium-Schranke wird bewusst NICHT hier geprüft — das passiert
    /// in der View vor dem Aufruf (braucht Query-Count und AppStorage).
    static func save(
        draft: EpisodeEditDraft,
        existingEpisode: Episode?,
        existingEpisodes: [Episode],
        coverChange: EpisodeCoverChange,
        in context: ModelContext
    ) -> EpisodeSaveOutcome {
        guard let episodeNumber = draft.parsedEpisodeNumber,
              let releaseYear = draft.parsedReleaseYear,
              let selectedUniverse = draft.selectedUniverse else {
            return .invalidInput
        }

        if hasDuplicateEpisodeNumber(
            in: selectedUniverse,
            episodeNumber: episodeNumber,
            existingEpisodes: existingEpisodes,
            editingEpisode: existingEpisode
        ) {
            return .duplicateNumber
        }

        if let episode = existingEpisode {
            let wasListened = episode.isListened
            let previousMoodKeys = Set(episode.moods.map(\.resolvedSyncKey))
            let newMoodKeys = Set(draft.selectedMoods.map(\.resolvedSyncKey))
            let previousNote = episode.personalNote
            let previousRating = episode.rating
            let previousStreamingURL = episode.streamingURL
            let previousListenStatus = episode.isListened
            episode.episodeNumber = episodeNumber
            episode.title = draft.title
            episode.releaseYear = releaseYear
            episode.personalNote = draft.personalNote.isEmpty ? nil : draft.personalNote
            episode.isListened = draft.isListened
            episode.rating = draft.rating
            episode.universe = selectedUniverse
            episode.moods = Array(draft.selectedMoods)
            if previousMoodKeys != newMoodKeys {
                episode.moodsUpdatedAt = .now
            }
            episode.streamingURL = draft.streamingURL.isEmpty ? nil : draft.streamingURL
            if episode.personalNote != previousNote { episode.noteUpdatedAt = .now }
            if episode.rating != previousRating { episode.ratingUpdatedAt = .now }
            if episode.streamingURL != previousStreamingURL { episode.streamingURLUpdatedAt = .now }
            if episode.isListened != previousListenStatus { episode.listenStatusUpdatedAt = .now }
            if episode.isHidden != draft.isHidden {
                episode.isHidden = draft.isHidden
                episode.hiddenUpdatedAt = .now
            }
            episode.refreshSyncKeyIfPossible()
            applyCover(coverChange, to: episode)

            if draft.isListened && !wasListened {
                episode.listenCount += 1
                episode.lastListenedAt = .now
                if episode.isBookmarked {
                    episode.isBookmarked = false
                    episode.bookmarkedUpdatedAt = .now
                }
            }
        } else {
            let newEpisode = Episode(
                episodeNumber: episodeNumber,
                title: draft.title,
                releaseYear: releaseYear,
                personalNote: draft.personalNote.isEmpty ? nil : draft.personalNote,
                isListened: draft.isListened,
                rating: draft.rating,
                universe: selectedUniverse,
                moods: Array(draft.selectedMoods)
            )
            newEpisode.streamingURL = draft.streamingURL.isEmpty ? nil : draft.streamingURL
            if !draft.selectedMoods.isEmpty {
                newEpisode.moodsUpdatedAt = .now
            }
            if draft.isListened {
                newEpisode.listenCount = 1
                newEpisode.lastListenedAt = .now
            }
            applyCover(coverChange, to: newEpisode)
            context.insert(newEpisode)
        }

        do {
            try context.save()
            return .saved
        } catch {
            return .saveFailed
        }
    }

    private static func applyCover(_ change: EpisodeCoverChange, to episode: Episode) {
        try? EpisodeCoverManager().apply(change, to: episode)
    }

    private static func hasDuplicateEpisodeNumber(
        in universe: Universe,
        episodeNumber: Int,
        existingEpisodes: [Episode],
        editingEpisode: Episode?
    ) -> Bool {
        existingEpisodes.contains { candidate in
            guard candidate.episodeNumber == episodeNumber else { return false }
            guard candidate.universe?.id == universe.id else { return false }
            if let editingEpisode {
                return candidate.id != editingEpisode.id
            }
            return true
        }
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/EpisodeEditSaveHandlerTests`
Expected: PASS (4 Tests)

- [ ] **Step 5: Commit**

```bash
git add EpisodeTracker/Features/EpisodeEdit/EpisodeEditSaveHandler.swift EpisodeTrackerTests/EpisodeEditSaveHandlerTests.swift
git commit -m "Add EpisodeEditSaveHandler with duplicate guard and sync timestamps"
```

---

### Task 3: EpisodeEditCoverHandler — Cover-Lifecycle

Bündelt Cover-State (`coverImage`, `removeCover`, `selectedPhotoItem`, `clipboardHasImage`) und leitet daraus die `EpisodeCoverChange` ab. `@Observable`, damit die View ihn als `@State` hält.

**Files:**
- Create: `EpisodeTracker/Features/EpisodeEdit/EpisodeEditCoverHandler.swift`
- Test: `EpisodeTrackerTests/EpisodeEditCoverHandlerTests.swift`

- [ ] **Step 1: Write the failing test**

```swift
// EpisodeTrackerTests/EpisodeEditCoverHandlerTests.swift
import XCTest
import UIKit
@testable import EpisodeTracker

@MainActor
final class EpisodeEditCoverHandlerTests: XCTestCase {
    func testDefaultChangeIsKeep() {
        let handler = EpisodeEditCoverHandler()
        XCTAssertEqual(handler.coverChange, .keep)
    }

    func testSelectingImageProducesReplaceChange() {
        let handler = EpisodeEditCoverHandler()
        let image = UIImage(systemName: "star")!

        handler.applyPickedImage(image)

        XCTAssertEqual(handler.coverChange, .replace(image))
        XCTAssertFalse(handler.removeCover)
        XCTAssertTrue(handler.hasNewImage)
    }

    func testRequestingRemovalProducesRemoveChange() {
        let handler = EpisodeEditCoverHandler()
        handler.applyPickedImage(UIImage(systemName: "star")!)

        handler.requestRemoval()

        XCTAssertEqual(handler.coverChange, .remove)
        XCTAssertNil(handler.coverImage)
        XCTAssertFalse(handler.hasNewImage)
    }
}
```

> **Note:** `EpisodeCoverChange` muss `Equatable` sein, damit `XCTAssertEqual` funktioniert. Falls es das noch nicht ist, in `EpisodeCoverManager.swift` `enum EpisodeCoverChange: Equatable` ergänzen — `UIImage` ist über Identität vergleichbar; für `.replace` reicht der Default-Compare nicht, deshalb manuell:
> ```swift
> enum EpisodeCoverChange: Equatable {
>     case keep
>     case remove
>     case replace(UIImage)
>     static func == (lhs: EpisodeCoverChange, rhs: EpisodeCoverChange) -> Bool {
>         switch (lhs, rhs) {
>         case (.keep, .keep), (.remove, .remove): return true
>         case let (.replace(a), .replace(b)): return a === b
>         default: return false
>         }
>     }
> }
> ```

- [ ] **Step 2: Run test to verify it fails**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/EpisodeEditCoverHandlerTests`
Expected: FAIL — `cannot find 'EpisodeEditCoverHandler' in scope`

- [ ] **Step 3: Write minimal implementation**

Falls nötig zuerst `EpisodeCoverChange: Equatable` machen (siehe Note in Step 1), dann:

```swift
// EpisodeTracker/Features/EpisodeEdit/EpisodeEditCoverHandler.swift
import SwiftUI
import PhotosUI

@MainActor
@Observable
final class EpisodeEditCoverHandler {
    var coverImage: UIImage?
    var removeCover = false
    var selectedPhotoItem: PhotosPickerItem?
    var clipboardHasImage = false

    var hasNewImage: Bool { coverImage != nil }

    /// Leitet die persistierte Cover-Änderung aus dem aktuellen State ab.
    var coverChange: EpisodeCoverChange {
        if removeCover { return .remove }
        if let coverImage { return .replace(coverImage) }
        return .keep
    }

    func applyPickedImage(_ image: UIImage) {
        coverImage = image
        selectedPhotoItem = nil
        removeCover = false
    }

    func requestRemoval() {
        coverImage = nil
        selectedPhotoItem = nil
        removeCover = true
    }

    func refreshClipboardAvailability() {
        clipboardHasImage = UIPasteboard.general.hasImages
    }

    func pasteFromClipboard() {
        if let image = UIPasteboard.general.image {
            applyPickedImage(image)
        }
    }

    func loadPickedItem(_ item: PhotosPickerItem) async {
        if let data = try? await item.loadTransferable(type: Data.self),
           let uiImage = UIImage(data: data) {
            coverImage = uiImage
            removeCover = false
        }
    }

    /// Ob für eine bestehende Folge ein sichtbares Cover existiert.
    func hasVisibleCover(for episode: Episode?) -> Bool {
        if removeCover { return false }
        if coverImage != nil { return true }
        guard let name = episode?.coverImageName, !name.isEmpty else { return false }
        return CoverImageStore().exists(name: name)
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/EpisodeEditCoverHandlerTests`
Expected: PASS (3 Tests)

- [ ] **Step 5: Commit**

```bash
git add EpisodeTracker/Features/EpisodeEdit/EpisodeEditCoverHandler.swift EpisodeTrackerTests/EpisodeEditCoverHandlerTests.swift EpisodeTracker/Features/Cover/EpisodeCoverManager.swift
git commit -m "Add EpisodeEditCoverHandler for cover lifecycle"
```

---

### Task 4: EpisodeEditView auf die neuen Typen umstellen

View nutzt `@State private var draft` und `@State private var coverHandler`, delegiert Save/Delete an `EpisodeEditSaveHandler`. Cover-bezogene Bindings/Closures gehen über den Handler. `populateInitialState` baut den Draft (behält die `prefillEntry`/`prefillUniverseName`/Default-1979-Pfade, die nicht in `init(episode:)` stecken). Dies ist ein UI-Verdrahtungstask ohne Unit-Test — Verifikation über Build + Smoke.

**Files:**
- Modify: `EpisodeTracker/EpisodeEditView.swift`

- [ ] **Step 1: Draft- und Cover-State einführen**

Ersetze die Einzel-`@State`-Properties für Formularfelder und Cover (Zeilen 22–43) durch:

```swift
    @State private var draft = EpisodeEditDraft()
    @State private var coverHandler = EpisodeEditCoverHandler()
    @State private var catalogMatch: CatalogEntry?
    @State private var yearSuggestions: [CatalogEntry] = []
    @State private var newMoodName: String = ""
    @State private var newMoodIcon: String = ""
    @State private var formValidationMessage: String?
    @State private var moodValidationMessage: String?
    @State private var showingDeleteConfirmation = false
    @State private var pendingCatalogRefreshKey: String?
    @Environment(\.scenePhase) private var scenePhase
```

Behalte `@Environment`, `@Query`, `@AppStorage` (Zeilen 6–16) und die `var episode/prefillEntry/prefillUniverseName` (18–20).

- [ ] **Step 2: Computed properties an den Draft anpassen**

Ersetze `parsedEpisodeNumber`/`parsedReleaseYear` durch Verweise auf den Draft und passe `canSave`, `duplicateEpisodeWarning`, `hasVisibleCover` an:

```swift
    private var isNew: Bool { episode == nil }
    private var hasVisibleCover: Bool { coverHandler.hasVisibleCover(for: episode) }
    private var canSave: Bool {
        draft.isComplete && canCreateEpisodeUnderCurrentPlan
    }
    private var duplicateEpisodeWarning: String? {
        guard isNew,
              let number = draft.parsedEpisodeNumber,
              let universe = draft.selectedUniverse,
              hasDuplicateEpisodeNumber(in: universe, episodeNumber: number)
        else { return nil }
        return "Folge \(number) existiert bereits in \(universe.name)."
    }
```

`existingEpisodeNumbersByCollection`, `titleSuggestions`, `activeCatalogCollectionNames`, `suggestedMoods`, `visibleMoods`, `activeUniverses`, `preferredCatalogUniverse`, `canCreateEpisodeUnderCurrentPlan` bleiben — ersetze darin nur `title` → `draft.title`, `selectedUniverse` → `draft.selectedUniverse`, `allEpisodes` bleibt. `hasDuplicateEpisodeNumber(in:episodeNumber:)` bleibt als private View-Helper (für das Warning) erhalten.

- [ ] **Step 3: Body-Bindings auf den Draft umstellen**

Im `body` alle `$episodeNumberText` → `$draft.episodeNumberText`, `$title` → `$draft.title`, `$releaseYearText` → `$draft.releaseYearText`, `$selectedUniverse` → `$draft.selectedUniverse`, `$isListened` → `$draft.isListened`, `$rating` → `$draft.rating`, `$personalNote` → `$draft.personalNote`, `$streamingURL` → `$draft.streamingURL`, `isHidden`-Toggle → `$draft.isHidden`. In der Cover-Section `selectedPhotoItem`/`coverImage`/`removeCover` über `coverHandler` ansprechen (Buttons rufen `coverHandler.pasteFromClipboard()` bzw. `coverHandler.requestRemoval()`; PhotosPicker bindet `$coverHandler.selectedPhotoItem`; Vorschau nutzt `coverHandler.coverImage`).

`selectedMoods` wird in `EpisodeMoodSection` als `selectedMoods:` (Wert) und in den Mood-Closures gesetzt — diese Closures (`toggleMoodSelection`, `addSuggestedMood`, `addMood`) mutieren jetzt `draft.selectedMoods` statt `selectedMoods`.

- [ ] **Step 4: Save/Delete und Lifecycle umstellen**

Sichern-Button:
```swift
                Button("Sichern") {
                    guard canCreateEpisodeUnderCurrentPlan else {
                        formValidationMessage = FreemiumAccess.limitReachedMessage()
                        return
                    }
                    let outcome = EpisodeEditSaveHandler.save(
                        draft: draft,
                        existingEpisode: episode,
                        existingEpisodes: allEpisodes,
                        coverChange: coverHandler.coverChange,
                        in: modelContext
                    )
                    switch outcome {
                    case .saved:
                        dismiss()
                    case .duplicateNumber:
                        formValidationMessage = "Diese Folgennummer ist in diesem Katalog schon vorhanden."
                    case .saveFailed:
                        formValidationMessage = "Speichern fehlgeschlagen. Bitte versuche es erneut."
                    case .invalidInput:
                        break
                    }
                }
                .disabled(!canSave)
```

`.onAppear` baut den Draft:
```swift
        .onAppear {
            SyncPreparation.prepare(context: modelContext)
            populateInitialState()
            refreshCatalogMatch()
            coverHandler.refreshClipboardAvailability()
        }
        .onChange(of: scenePhase) { _, newPhase in
            if newPhase == .active {
                coverHandler.refreshClipboardAvailability()
            }
        }
        .onChange(of: coverHandler.selectedPhotoItem) { _, newItem in
            guard let newItem else { return }
            Task { await coverHandler.loadPickedItem(newItem) }
        }
```

`populateInitialState` neu (ersetzt 367–394):
```swift
    private func populateInitialState() {
        if let episode {
            draft = EpisodeEditDraft(episode: episode, universes: universes)
        } else if let prefillEntry {
            draft.episodeNumberText = String(prefillEntry.number)
            draft.title = prefillEntry.title
            draft.releaseYearText = String(prefillEntry.releaseYear)
            if let prefillUniverseName {
                draft.selectedUniverse = universes.first {
                    $0.name.caseInsensitiveCompare(prefillUniverseName) == .orderedSame
                } ?? universes.first
            } else {
                draft.selectedUniverse = preferredCatalogUniverse ?? universes.first
            }
        } else if draft.releaseYearText.isEmpty {
            draft.releaseYearText = "1979"
            draft.selectedUniverse = preferredCatalogUniverse ?? universes.first
        }
    }
```

Delete-Dialog bleibt unverändert (nutzt `EpisodeDeleteHelper`). Lösche die alten `save()`, `applyCoverChange()` Methoden aus der View (jetzt im SaveHandler). `refreshCatalogMatch`, `refreshYearSuggestions`, `applyCatalogMatch`, `applySuggestedEntry`, Mood-Helper bleiben, angepasst auf `draft.*`.

- [ ] **Step 5: Build verifizieren**

Run:
```bash
xcodebuild -project EpisodeTracker.xcodeproj -scheme EpisodeTracker \
  -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' build 2>&1 | tail -20
```
Expected: `BUILD SUCCEEDED`

- [ ] **Step 6: Voller Testlauf (Regression)**

Run: voller Testlauf (siehe Header).
Expected: `TEST SUCCEEDED` — bestehende Tests bleiben grün.

- [ ] **Step 7: Manueller Smoke**

Simulator: Neue Folge anlegen (mit Cover aus Zwischenablage), bestehende Folge bearbeiten (Titel + als gehört markieren → listenCount steigt, Bookmark verschwindet), Cover ersetzen/entfernen, Duplikat-Nummer → Warnung. EditView-Datei prüfen: deutlich unter 940 Zeilen (Ziel ~400 im Haupttyp; private Sub-Views bleiben).

- [ ] **Step 8: Commit**

```bash
git add EpisodeTracker/EpisodeEditView.swift
git commit -m "Wire EpisodeEditView to draft, save and cover handlers"
```

---

## Phase 2 — Widget-Snapshot rausziehen

### Task 5: WidgetSnapshotCoordinator — Signatur-Dedupe und Schreiben

Zieht Signatur-Berechnung + `writeSnapshot` aus `WidgetSyncObserverView`. Der Coordinator merkt sich die letzte Signatur und schreibt nur bei echter Änderung.

**Files:**
- Create: `EpisodeTracker/Features/Widget/WidgetSnapshotCoordinator.swift`
- Test: `EpisodeTrackerTests/WidgetSnapshotCoordinatorTests.swift`

- [ ] **Step 1: Write the failing test**

```swift
// EpisodeTrackerTests/WidgetSnapshotCoordinatorTests.swift
import XCTest
@testable import EpisodeTracker

@MainActor
final class WidgetSnapshotCoordinatorTests: XCTestCase {
    func testSignatureChangesWhenEpisodeListenStatusChanges() {
        let universe = Universe(name: "Die drei ???")
        let episode = Episode(episodeNumber: 1, title: "A", releaseYear: 1979, universe: universe)

        let before = WidgetSnapshotCoordinator.signature(
            libraryTitle: "Meine Hörspiele", universes: [universe], episodes: [episode]
        )
        episode.isListened = true
        let after = WidgetSnapshotCoordinator.signature(
            libraryTitle: "Meine Hörspiele", universes: [universe], episodes: [episode]
        )

        XCTAssertNotEqual(before, after)
    }

    func testSignatureStableForUnchangedData() {
        let universe = Universe(name: "Die drei ???")
        let episode = Episode(episodeNumber: 1, title: "A", releaseYear: 1979, universe: universe)

        let a = WidgetSnapshotCoordinator.signature(
            libraryTitle: "T", universes: [universe], episodes: [episode]
        )
        let b = WidgetSnapshotCoordinator.signature(
            libraryTitle: "T", universes: [universe], episodes: [episode]
        )

        XCTAssertEqual(a, b)
    }

    func testRefreshSkipsWriteWhenSignatureUnchanged() {
        let coordinator = WidgetSnapshotCoordinator()
        let universe = Universe(name: "Die drei ???")
        let episode = Episode(episodeNumber: 1, title: "A", releaseYear: 1979, universe: universe)
        var writeCount = 0
        coordinator.writeHook = { writeCount += 1 }

        coordinator.refresh(libraryTitle: "T", universes: [universe], episodes: [episode])
        coordinator.refresh(libraryTitle: "T", universes: [universe], episodes: [episode])

        XCTAssertEqual(writeCount, 1, "Zweiter Refresh ohne Änderung schreibt nicht erneut")

        episode.isListened = true
        coordinator.refresh(libraryTitle: "T", universes: [universe], episodes: [episode])
        XCTAssertEqual(writeCount, 2, "Echte Änderung schreibt erneut")
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/WidgetSnapshotCoordinatorTests`
Expected: FAIL — `cannot find 'WidgetSnapshotCoordinator' in scope`

- [ ] **Step 3: Write minimal implementation**

Die `episodeSignature`/`universeSignature`-Logik wird aus `WidgetSyncObserverView` (Zeilen 10–31) hierher übernommen und um den Schreib-Dedupe + einen Test-Hook erweitert.

```swift
// EpisodeTracker/Features/Widget/WidgetSnapshotCoordinator.swift
import Foundation

@MainActor
@Observable
final class WidgetSnapshotCoordinator {
    private var lastSignature: String?

    /// Test-Hook: wird statt des echten Snapshot-Writes aufgerufen, wenn gesetzt.
    var writeHook: (() -> Void)?

    func refresh(libraryTitle: String, universes: [Universe], episodes: [Episode]) {
        let signature = Self.signature(
            libraryTitle: libraryTitle,
            universes: universes,
            episodes: episodes
        )
        guard signature != lastSignature else { return }
        lastSignature = signature

        if let writeHook {
            writeHook()
        } else {
            WidgetSyncStore.writeSnapshot(
                libraryTitle: libraryTitle,
                universes: universes,
                episodes: episodes
            )
        }
    }

    static func signature(libraryTitle: String, universes: [Universe], episodes: [Episode]) -> String {
        let episodeSignature = episodes.map { episode in
            let universeName = episode.universe?.name ?? ""
            let rating = episode.rating.map(String.init) ?? ""
            let listenedAt = episode.lastListenedAt?.timeIntervalSince1970.description ?? ""
            return [
                episode.id.uuidString,
                String(episode.episodeNumber),
                episode.title,
                String(episode.releaseYear),
                universeName,
                episode.isListened ? "1" : "0",
                episode.isBookmarked ? "1" : "0",
                rating,
                listenedAt,
            ].joined(separator: "|")
        }
        .joined(separator: "\n")

        let universeSignature = universes.map(\.name).joined(separator: "\n")
        return [libraryTitle, universeSignature, episodeSignature].joined(separator: "\u{1F}")
    }
}
```

> **Note:** Die Signatur ergänzt `isBookmarked` (das `WidgetSyncStore` seit V1.10 schreibt, in der alten Observer-Signatur aber fehlte) — so triggert ein Bookmark-Wechsel jetzt korrekt einen Snapshot-Write. Das ist ein bewusster Bugfix, kein reiner Move.

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/WidgetSnapshotCoordinatorTests`
Expected: PASS (3 Tests)

- [ ] **Step 5: Commit**

```bash
git add EpisodeTracker/Features/Widget/WidgetSnapshotCoordinator.swift EpisodeTrackerTests/WidgetSnapshotCoordinatorTests.swift
git commit -m "Add WidgetSnapshotCoordinator with signature dedupe"
```

---

### Task 6: Observer-View verschlanken und Coordinator einhängen

`WidgetSyncObserverView` wird zum dünnen Query-Adapter; die App hält den Coordinator als `@State`.

**Files:**
- Modify: `EpisodeTracker/Features/Widget/WidgetSyncObserverView.swift`
- Modify: `EpisodeTracker/Shared/App/EpisodeTrackerApp.swift`

- [ ] **Step 1: Observer-View auf reinen Query-Adapter reduzieren**

Ersetze den kompletten Inhalt von `WidgetSyncObserverView.swift` durch:

```swift
import SwiftUI
import SwiftData

/// Dünner Query-Adapter: hält die SwiftData-Queries für Live-Reaktivität
/// und reicht Änderungen an den Coordinator durch. Keine Snapshot-Logik hier.
struct WidgetSyncObserverView: View {
    @Environment(\.scenePhase) private var scenePhase
    @AppStorage("libraryTitle") private var libraryTitle: String = "Meine Hörspiele"
    @Query(sort: \Episode.episodeNumber) private var episodes: [Episode]
    @Query(sort: \Universe.name) private var universes: [Universe]

    let coordinator: WidgetSnapshotCoordinator

    private var signature: String {
        WidgetSnapshotCoordinator.signature(
            libraryTitle: libraryTitle,
            universes: universes,
            episodes: episodes
        )
    }

    var body: some View {
        Color.clear
            .frame(width: 0, height: 0)
            .task { refresh() }
            .onChange(of: signature) { _, _ in refresh() }
            .onChange(of: scenePhase) { _, newPhase in
                if newPhase == .active { refresh() }
            }
            .allowsHitTesting(false)
            .accessibilityHidden(true)
    }

    private func refresh() {
        coordinator.refresh(libraryTitle: libraryTitle, universes: universes, episodes: episodes)
    }
}
```

> Nutzt jetzt eine einzelne `signature`-`onChange` statt drei separater (`episodeSignature`/`universeSignature`/`libraryTitle`) — die kombinierte Signatur deckt alle drei ab.

- [ ] **Step 2: Coordinator in der App halten und übergeben**

In `EpisodeTrackerApp.swift`:

```swift
    @State private var syncCoordinator: SyncCoordinator
    @State private var widgetSnapshotCoordinator = WidgetSnapshotCoordinator()
```

und im Body:
```swift
                WidgetSyncObserverView(coordinator: widgetSnapshotCoordinator)
```

- [ ] **Step 3: Build verifizieren**

Run: `xcodebuild ... build`
Expected: `BUILD SUCCEEDED`

- [ ] **Step 4: Voller Testlauf**

Run: voller Testlauf.
Expected: `TEST SUCCEEDED`

- [ ] **Step 5: Manueller Smoke (Widget)**

Folge als gehört markieren / Bookmark setzen → Widget „Als nächstes" und „Zufällige Folge" aktualisieren sich (ggf. App in Hintergrund/Vordergrund). Snapshot-JSON wird geschrieben.

- [ ] **Step 6: Commit**

```bash
git add EpisodeTracker/Features/Widget/WidgetSyncObserverView.swift EpisodeTracker/Shared/App/EpisodeTrackerApp.swift
git commit -m "Slim widget observer to query adapter, hold coordinator in app"
```

---

## Phase 3 — Katalog-Bibliothek-Matching zentralisieren

### Task 7: CatalogLibraryMatcher — eine Quelle der Wahrheit

Konsolidiert die dreifach duplizierte Normalisierung und die Missing-/Existing-Berechnung. Fixt den bestätigten Drift (SmartList nutzt heute `.lowercased()` ohne Trim).

**Files:**
- Create: `EpisodeTracker/Features/Catalog/CatalogLibraryMatcher.swift`
- Test: `EpisodeTrackerTests/CatalogLibraryMatcherTests.swift`

- [ ] **Step 1: Write the failing test**

```swift
// EpisodeTrackerTests/CatalogLibraryMatcherTests.swift
import XCTest
@testable import EpisodeTracker

final class CatalogLibraryMatcherTests: XCTestCase {
    private func entry(_ number: Int, _ title: String, _ collection: String) -> CatalogEntry {
        CatalogEntry(number: number, title: title, releaseYear: 2020, collectionName: collection)
    }

    func testNormalizationTrimsAndLowercases() {
        XCTAssertEqual(CatalogLibraryMatcher.normalizedCollectionKey("  Die DREI ???  "), "die drei ???")
    }

    func testExistingNumbersGroupedByNormalizedKey() {
        let u = Universe(name: " Die drei ??? ")
        let episodes = [
            Episode(episodeNumber: 1, title: "A", releaseYear: 1979, universe: u),
            Episode(episodeNumber: 2, title: "B", releaseYear: 1979, universe: u),
        ]

        let map = CatalogLibraryMatcher.existingNumbersByCollection(libraryEpisodes: episodes)

        XCTAssertEqual(map["die drei ???"], [1, 2])
    }

    func testMissingEntriesExcludesNumbersAlreadyInLibrary() {
        let u = Universe(name: "Die drei ???")
        let library = [
            Episode(episodeNumber: 1, title: "A", releaseYear: 1979, universe: u),
        ]
        let catalog = [
            entry(1, "A", "Die drei ???"),
            entry(2, "B", "Die drei ???"),
            entry(3, "C", "Die drei ???"),
        ]

        let missing = CatalogLibraryMatcher.missingEntries(catalogEntries: catalog, libraryEpisodes: library)

        XCTAssertEqual(missing.map(\.entry.number), [2, 3])
    }

    func testMatchingToleratesWhitespaceDriftBetweenLibraryAndCatalog() {
        // Regression: vorher matchte SmartList mit .lowercased() ohne Trim,
        // Autocomplete mit trim+lowercase -> Inkonsistenz.
        let u = Universe(name: " Die drei ??? ")
        let library = [Episode(episodeNumber: 1, title: "A", releaseYear: 1979, universe: u)]
        let catalog = [entry(1, "A", "Die drei ???"), entry(2, "B", "Die drei ???")]

        let missing = CatalogLibraryMatcher.missingEntries(catalogEntries: catalog, libraryEpisodes: library)

        XCTAssertEqual(missing.map(\.entry.number), [2], "Folge 1 gilt trotz Whitespace als vorhanden")
    }

    func testEmptyLibraryYieldsNoMissingEntries() {
        // missingEntries betrachtet nur Collections, die in der Bibliothek vertreten sind.
        let catalog = [entry(1, "A", "Die drei ???")]
        let missing = CatalogLibraryMatcher.missingEntries(catalogEntries: catalog, libraryEpisodes: [])
        XCTAssertTrue(missing.isEmpty)
    }
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/CatalogLibraryMatcherTests`
Expected: FAIL — `cannot find 'CatalogLibraryMatcher' in scope`

- [ ] **Step 3: Write minimal implementation**

Logik aus `SmartListDefinition.missingCatalogEntries` (Zeilen 353–384) übernommen, aber durchgängig über `normalizedCollectionKey`.

```swift
// EpisodeTracker/Features/Catalog/CatalogLibraryMatcher.swift
import Foundation

/// Eine Quelle der Wahrheit für die Frage: Welche Katalogfolge hat der Nutzer
/// bereits in der Bibliothek? Konsolidiert Normalisierung und Missing-/Existing-
/// Berechnung, die zuvor in SmartListDefinition, CatalogTitleAutocomplete und
/// EpisodeCatalog dupliziert (und leicht abweichend) implementiert war.
enum CatalogLibraryMatcher {
    nonisolated static func normalizedCollectionKey(_ value: String) -> String {
        value.trimmingCharacters(in: .whitespacesAndNewlines).lowercased()
    }

    nonisolated static func existingNumbersByCollection(libraryEpisodes: [Episode]) -> [String: Set<Int>] {
        Dictionary(grouping: libraryEpisodes) {
            normalizedCollectionKey($0.universe?.name ?? "")
        }
        .mapValues { episodes in Set(episodes.map(\.episodeNumber)) }
    }

    nonisolated static func missingEntries(
        catalogEntries: [CatalogEntry],
        libraryEpisodes: [Episode]
    ) -> [(universeName: String, entry: CatalogEntry)] {
        let libraryByCollection = existingNumbersByCollection(
            libraryEpisodes: libraryEpisodes.filter { $0.universe != nil }
        )
        let catalogByCollection = Dictionary(grouping: catalogEntries.filter { $0.collectionName != nil }) {
            normalizedCollectionKey($0.collectionName ?? "")
        }

        var results: [(universeName: String, entry: CatalogEntry)] = []

        for (collectionKey, catalogEpisodes) in catalogByCollection {
            let libraryNumbers = libraryByCollection[collectionKey] ?? []
            guard !libraryNumbers.isEmpty else { continue }

            let displayName = catalogEpisodes.first?.collectionName ?? collectionKey

            var seenNumbers = Set<Int>()
            let uniqueEpisodes = catalogEpisodes.filter { seenNumbers.insert($0.number).inserted }

            let missing = uniqueEpisodes
                .filter { !libraryNumbers.contains($0.number) }
                .sorted { $0.number < $1.number }

            for entry in missing {
                results.append((displayName, entry))
            }
        }

        results.sort {
            if $0.universeName != $1.universeName {
                return $0.universeName.localizedCompare($1.universeName) == .orderedAscending
            }
            return $0.entry.number < $1.entry.number
        }
        return results
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `xcodebuild ... test -only-testing:EpisodeTrackerTests/CatalogLibraryMatcherTests`
Expected: PASS (5 Tests)

- [ ] **Step 5: Commit**

```bash
git add EpisodeTracker/Features/Catalog/CatalogLibraryMatcher.swift EpisodeTrackerTests/CatalogLibraryMatcherTests.swift
git commit -m "Add CatalogLibraryMatcher as single source for catalog-library matching"
```

---

### Task 8: Konsumenten auf den Matcher umstellen

**Files:**
- Modify: `EpisodeTracker/Features/SmartList/SmartListDefinition.swift:353-384`
- Modify: `EpisodeTracker/Features/Catalog/CatalogTitleAutocomplete.swift`
- Modify: `EpisodeTracker/EpisodeEditView.swift`

- [ ] **Step 1: SmartListDefinition.missingCatalogEntries delegieren**

Ersetze den Body von `missingCatalogEntries(catalogEntries:libraryEpisodes:)` (353–384) durch:

```swift
    static func missingCatalogEntries(catalogEntries: [CatalogEntry], libraryEpisodes: [Episode]) -> [(universeName: String, entry: CatalogEntry)] {
        CatalogLibraryMatcher.missingEntries(catalogEntries: catalogEntries, libraryEpisodes: libraryEpisodes)
    }
```

`nextFromCatalog` (348–352) bleibt unverändert — es ruft `missingCatalogEntries` auf.

- [ ] **Step 2: CatalogTitleAutocomplete-Normalisierung konsolidieren**

In `CatalogTitleAutocomplete.swift` ersetze alle vier `normalizedKey(...)`-Aufrufe (Zeilen 15, 24, 41) durch `CatalogLibraryMatcher.normalizedCollectionKey(...)` und entferne die lokale `normalizedKey`-Methode (48–50).

> Die Signatur von `suggestions(...)` bleibt unverändert — `existingEpisodeNumbersByCollection` wird weiterhin vom Aufrufer übergeben. Die bestehenden `CatalogTitleAutocompleteTests` müssen unverändert grün bleiben.

- [ ] **Step 3: EditView baut existingNumbers über den Matcher**

In `EpisodeEditView.swift` ersetze die computed property `existingEpisodeNumbersByCollection` (147–154) durch:

```swift
    private var existingEpisodeNumbersByCollection: [String: Set<Int>] {
        CatalogLibraryMatcher.existingNumbersByCollection(libraryEpisodes: allEpisodes)
    }
```

Und in `activeCatalogCollectionNames` (138–145) `CatalogTitleAutocomplete.normalizedKey` → `CatalogLibraryMatcher.normalizedCollectionKey`. (Falls `EpisodeCatalog.normalizedKey`, Zeilen 258–264, semantisch identisch genutzt wird — es betrifft nur Source-`id`-Vergleiche, nicht Collection-Namen — bleibt es unangetastet; es ist ein separater Belang.)

- [ ] **Step 4: Build verifizieren**

Run: `xcodebuild ... build`
Expected: `BUILD SUCCEEDED`

- [ ] **Step 5: Regression — betroffene Testklassen**

Run:
```bash
xcodebuild ... test \
  -only-testing:EpisodeTrackerTests/SmartListTests \
  -only-testing:EpisodeTrackerTests/CatalogTitleAutocompleteTests \
  -only-testing:EpisodeTrackerTests/CatalogLibraryMatcherTests 2>&1 | tail -20
```
Expected: `TEST SUCCEEDED` — Verhaltensgleichheit bestätigt.

- [ ] **Step 6: Commit**

```bash
git add EpisodeTracker/Features/SmartList/SmartListDefinition.swift EpisodeTracker/Features/Catalog/CatalogTitleAutocomplete.swift EpisodeTracker/EpisodeEditView.swift
git commit -m "Route catalog-library matching through CatalogLibraryMatcher"
```

---

## Phase 4 — Performance-Smoke + Release-Gate

### Task 9: Performance-Check, Release-Gate und Versionsbump

Kein neues Feature — bewusste Verifikation und Versionsfinalisierung. Findet der Check ein konkretes Problem, wird es punktuell gefixt (eigener Commit); ohne Befund keine spekulative Optimierung.

**Files:**
- Modify: `EpisodeTracker.xcodeproj/project.pbxproj` (Versions-/Build-Nummer)

- [ ] **Step 1: Voller Testlauf grün**

Run: voller Testlauf (Header).
Expected: `TEST SUCCEEDED`

- [ ] **Step 2: Release-Build grün (Debug- und Non-Debug-Pfade)**

Run:
```bash
xcodebuild -project EpisodeTracker.xcodeproj -scheme EpisodeTracker \
  -configuration Release \
  -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' build 2>&1 | tail -20
```
Expected: `BUILD SUCCEEDED` — keine `#if DEBUG`-Hilfstypen im Release-Pfad referenziert.

- [ ] **Step 3: Performance-/Regression-Smoke**

Im Simulator (große Bibliothek, 200+ Folgen — falls vorhanden über Backup-Import) prüfen:

| Pfad | Erwartung |
|------|-----------|
| EpisodeListView / SmartListDetailView scrollen | flüssig, keine sichtbaren Hänger |
| StatisticsView öffnen/wechseln | Snapshot berechnet sich nicht spürbar bei jedem Re-Render |
| Widget-Snapshot | schreibt nur bei echter Änderung (Coordinator-Dedupe greift) |
| EpisodeDetailView Hero-Cover öffnen | Blur-Layer flüssig, kein Ruckeln |
| EpisodeEditView | Anlegen/Bearbeiten/Cover/Save identisch zu vor dem Refactor |

Befund → punktueller Fix + eigener Commit, danach Step 1/2 erneut. Kein Befund → weiter.

- [ ] **Step 4: iPhone-/iPad-Smoke**

iPhone und iPad (Split-View): Listen, Statistik, Widgets, Detailansicht, EditView, App-Icon-Wechsel in Settings (alle vier Varianten).

- [ ] **Step 5: Versions-/Build-Nummer auf 1.11 (19) ziehen**

In `project.pbxproj` `MARKETING_VERSION` auf `1.11` und `CURRENT_PROJECT_VERSION` auf `19` für **App- und Widget-Target gemeinsam** setzen (CFBundleVersion-Mismatch vermeiden — siehe Lessons Learned).

Run nach Änderung: `xcodebuild ... build`
Expected: `BUILD SUCCEEDED`

- [ ] **Step 6: Commit**

```bash
git add EpisodeTracker.xcodeproj/project.pbxproj
git commit -m "Bump version to 1.11 (19)"
```

- [ ] **Step 7: TestFlight-Upgrade-Gate (manuell, vor Einreichung)**

> Nicht automatisierbar, kein Dev-Build über App-Store-Version installieren (Sandbox-Container-Verlust, siehe Lessons Learned). Über TestFlight oder Xcode Archive mit Ad-Hoc-Export: Upgrade V1.10 → V1.11, Container überlebt, neue Felder/Cover intakt, Widgets aktualisieren.

---

## Abschluss

Nach Phase 4: `finishing-a-development-branch` für Merge-Entscheidung. Roadmap P9c, Release.md und Plan.md im Vault auf „V1.11 abgeschlossen" aktualisieren (Versionshistorie-Tabelle, Repo-Stand/Tag).
